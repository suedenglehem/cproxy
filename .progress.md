# Progress

## Status
CUTOVER DONE — cproxy.exe Windows service is now THE proxy on port 8787 (upstream tr4:8080), verified end-to-end with a real request returning "OK". Old standalone proxy killed, old Startup autostart removed. settings.json needed no change (already pointed at 8787). Remaining: optional commit of all changes; decide H:-repo vs C:-live-install duplication.

## Last updated
2026-09-13 ~06:50 — cleaned up C:\Users\andrei\claude-llama-proxy (removed stale proxy.log + one-off logs\svc-*.log). Service still RUNNING on 8787, health green. Earlier this session: cutover to the service on 8787 completed and verified end-to-end.

## Completed work
- **Health monitor in proxy.mjs**: loads `proxy.env` from its own dir (real env wins); CLI flags `--port/--host/--upstream` beat everything; polls `{UPSTREAM}/models` every `HEALTH_POLL_MS` (default 5000, 3s timeout, any HTTP response = alive); logs ONE line on down and one on up; while dead `/v1/messages` → 503 + Anthropic-shaped "not available yet" message with Retry-After; `/health` now returns `upstream_up`.
- **service.cjs** (node-windows wrapper): subcommands install/uninstall/start/stop/restart/status/set. SCM id is `cproxy.exe`, display name `cproxy`. PORT/HOST/(UPSTREAM) are startup params baked into `daemon\cproxy.xml` AND registry `...\Services\cproxy.exe\Parameters`. `set --port N [--host H] [--upstream URL|""]` rewrites both + restarts. Fixed node-windows' broken `restart()` with a hand-rolled `robustRestart` (stop → poll sc.exe until STOPPED → start). All Service instantiations carry `script` (node-windows needs it to derive its dir).
- **vendor/node_modules/node-windows** installed locally (`npm install --prefix vendor node-windows`) so the service works offline. Removed a stray debug console.log from winsw.js.
- **.gitignore**: added `logs/`, `daemon/`, `runtime/`.
- **Service is registered & running** on this machine at C:\Users\andrei\claude-llama-proxy (the live install location), now on port **8787**, upstream tr4:8080.
- **Cutover to the service completed**: killed old standalone proxy (`node proxy.mjs`, PID 26668) → freed 8787; `node service.cjs set --port 8787` (elevated) moved the service onto 8787 via robustRestart; verified `/health` green + real Anthropic request returned `"OK"` with correct usage. Removed old Startup autostart `ClaudeLlamaProxy.cmd`. Fixed install.cmd step 7 to remove any of cproxy.cmd / ClaudeLlamaProxy.cmd / start-proxy.cmd from Startup (the original only knew cproxy.cmd).
- **Cleaned up C:\Users\andrei\claude-llama-proxy**: deleted stale root `proxy.log` (111 KB, written by the retired Startup flow — nothing writes it now; live request logging goes to `logs\cproxy.err.log`) and one-off `logs\svc-*.log` files from this session's elevated helper .cmd files. Kept core files + daemon/ + live logs/ + vendor/. Service still RUNNING on 8787 after cleanup.
- **install.cmd reworked** to the service flow: node check → npm i -g claude-code → write settings.json → write proxy.env → `npm install --prefix vendor node-windows` → register+start service (admin detection via `net session`, UAC fallback via `powershell Start-Process ... -Verb RunAs`) → remove old Startup-folder autostart. Idempotent; takes `[UPSTREAM_URL] [MODEL_NAME] [PORT]`.
- **README.md rewritten** for the Windows-service model: service.cjs command reference, editable startup params (daemon\cproxy.xml + registry), health monitor / 503 behavior, HEALTH_POLL_MS, live-install location note. proxy.env.example updated with HEALTH_POLL_MS + precedence note. All three synced to C:\Users\andrei\claude-llama-proxy.

## Unfinished / in progress
- **Optional**: commit all changes in the H: dev repo (proxy.mjs, service.cjs, install.cmd, README.md, proxy.env.example, .gitignore). Nothing committed yet — last commit is 5b92bdf.
- Decide: keep H:\vscode\cproxy as the dev repo and C:\...\claude-llama-proxy as the live install? (Currently code is duplicated/synced by hand between them.)

## Important decisions
- **node-windows** chosen over a C# host or NSSM (user's pick). Dep is installer-time only; proxy.mjs stays zero-dep at runtime.
- **Live install location = C:\Users\andrei\claude-llama-proxy** (system drive, per user), NOT the H: dev repo. Service files (daemon/, logs/) live there.
- **Service now owns port 8787** (was temporarily on 8788 for side-by-side validation). Because `settings.json` already pointed ANTHROPIC_BASE_URL at 8787, moving the *service* onto 8787 meant Claude Code needed zero config changes — it just hit the service instead of the old script. The old standalone proxy + its Startup autostart are retired.
- **Debug LLM** = LM Studio at 127.0.0.1:1234, model `qwen3.8-27b-uncensored`. Don't kill it (PID was 13292). tr4:8080 is Claude's real backend — don't kill node.exe globally or restart :8787 mid-request.
- Health probe treats ANY HTTP response from /models as alive; only network error/timeout = dead. 503 (not 502) so Claude Code auto-retries.

## Failing tests / errors
None currently. (Earlier: node-windows `restart()` left service STOPPED — fixed with robustRestart; bare `new Service({name})` crashed on missing script path — fixed by always passing script.)

## Next step
Commit all changes in H:\vscode\cproxy (git add proxy.mjs service.cjs install.cmd README.md proxy.env.example .gitignore; commit with a message describing the Windows-service rework + health monitor, ending with the Co-Authored-By line). Then optionally prove the installer end-to-end by re-running `install.cmd` from C:\Users\andrei\claude-llama-proxy (it's idempotent — it will detect the existing service and just confirm params).
